// Para generar un comentario debemos usar "//"

// Esto es un comentario de una sola línea

/* 
Esto es un comentario
en varias líneas
 */

let a = 10;
var b = 10;
const c = 10;

/*
!! Const hace referencia a un valor en memoria, y ese valor
!! no va a poder cambiar nunca
 */

//! No se utiliza más "VAR" => Ya que te permite reasignar el valor de la variable
//! y redeclarar la misma variable

// Declaración de muchas variables en una sola línea
let y = 10,
    x = 20,
    z = 30;

const d = y + x + z;

// Imprimir en consola la constante "d"
console.log(d);